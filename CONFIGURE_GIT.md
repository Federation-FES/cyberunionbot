# ⚙️ Настройка Git пользователя (альтернативные способы)

## Проблема:
Команда "Git: Set User Email" не найдена в Command Palette.

## Решение:

### Способ 1: Через настройки VS Code ⭐

1. **Откройте настройки:**
   - Нажмите `Ctrl + ,` (запятая)
   - Или: File → Preferences → Settings

2. **В поиске настроек введите:**
   ```
   git user
   ```

3. **Найдите и заполните:**
   - `Git: User Name` - введите ваше имя
   - `Git: User Email` - введите ваш email

4. **Сохраните настройки** (автоматически)

---

### Способ 2: Через settings.json

1. **Откройте Command Palette** (`Ctrl + Shift + P`)

2. **Введите:**
   ```
   Preferences: Open User Settings (JSON)
   ```

3. **Добавьте в файл:**
   ```json
   {
     "git.user.name": "Ваше Имя",
     "git.user.email": "your.email@example.com"
   }
   ```

4. **Сохраните файл** (`Ctrl + S`)

---

### Способ 3: Напрямую в Git конфигурации

1. **Откройте терминал VS Code** (`Ctrl + ~`)

2. **Проверьте, есть ли Git:**
   ```powershell
   where.exe git
   ```

3. **Если Git найден, выполните:**
   ```powershell
   git config --global user.name "Ваше Имя"
   git config --global user.email "your.email@example.com"
   ```

4. **Если Git не найден**, используйте Способ 1 или 2.

---

### Способ 4: Создать файл .gitconfig вручную

1. **Откройте папку пользователя:**
   - `C:\Users\admin\`

2. **Создайте файл `.gitconfig`** (если его нет)

3. **Добавьте в файл:**
   ```ini
   [user]
       name = Ваше Имя
       email = your.email@example.com
   ```

4. **Сохраните файл**

---

## ✅ Проверка:

После настройки попробуйте снова сделать коммит в Source Control. Ошибка должна исчезнуть.

---

## 💡 Рекомендация:

**Используйте Способ 1** (через настройки VS Code) - это самый простой и надежный способ.
