# 🔧 Исправление проблемы с Git репозиторием

## Проблема:
После инициализации Git видно слишком много файлов (10000+), включая файлы Cursor и временные файлы.

## Решение:

### Шаг 1: Убедитесь, что репозиторий в правильной папке

1. **Откройте терминал VS Code** (`Ctrl + ~`)

2. **Проверьте текущую папку:**
   ```powershell
   pwd
   ```
   Должно быть: `c:\Users\admin\cyberunionbot`

3. **Проверьте, есть ли `.git` в текущей папке:**
   ```powershell
   Test-Path .git
   ```
   Если `False` - репозиторий не в этой папке!

### Шаг 2: Удалите неправильный репозиторий и создайте новый

**Если `.git` НЕ в папке `cyberunionbot`:**

1. **Найдите, где находится `.git`:**
   ```powershell
   Get-ChildItem -Path C:\Users\admin -Filter .git -Recurse -Directory -ErrorAction SilentlyContinue | Select-Object FullName
   ```

2. **Удалите неправильный `.git`** (будьте осторожны!):
   ```powershell
   # НЕ удаляйте, если не уверены! Сначала проверьте путь выше
   # Remove-Item -Path "ПУТЬ_К_.GIT" -Recurse -Force
   ```

3. **Инициализируйте репозиторий в правильной папке:**
   ```powershell
   cd c:\Users\admin\cyberunionbot
   git init
   ```

### Шаг 3: Обновите `.gitignore`

Я уже обновил ваш `.gitignore` файл, чтобы игнорировать:
- Файлы Cursor (`.cursor/`, `agent-transcriptions/`, и т.д.)
- Временные файлы
- Файлы из папки `AppData`

### Шаг 4: Очистите индекс Git

1. **Удалите все файлы из индекса:**
   ```powershell
   git rm -r --cached .
   ```

2. **Добавьте файлы заново (только из папки проекта):**
   ```powershell
   git add .
   ```

3. **Проверьте статус:**
   ```powershell
   git status
   ```
   Теперь должны быть видны только файлы из `cyberunionbot`!

### Шаг 5: В VS Code

1. **Обновите панель Source Control:**
   - Закройте и откройте панель Source Control (Ctrl+Shift+G)
   - Или перезагрузите окно VS Code (Ctrl+R)

2. **Теперь должны быть видны только файлы проекта!**

---

## Альтернативный способ (если ничего не помогает):

1. **Закройте VS Code**

2. **Удалите все `.git` папки из `C:\Users\admin` и подпапок** (будьте осторожны!)

3. **Откройте VS Code в папке `cyberunionbot`**

4. **Инициализируйте репозиторий:**
   - `Ctrl + Shift + P` → `Git: Initialize Repository`
   - Выберите папку `cyberunionbot`

5. **Готово!**
