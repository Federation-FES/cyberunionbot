# 🔧 Исправление ошибки push

## Проблема:
```
[rejected] main -> main (fetch first)
Updates were rejected because the remote contains work that you do not have locally
```

## Решение:

### Вариант 1: Pull + Push (РЕКОМЕНДУЕТСЯ) ⭐

1. **Получите изменения с GitHub:**
   ```powershell
   git pull origin main --allow-unrelated-histories
   ```

2. **Если есть конфликты** - разрешите их, затем:
   ```powershell
   git add .
   git commit -m "Merge remote changes"
   ```

3. **Отправьте изменения:**
   ```powershell
   git push -u origin main
   ```

---

### Вариант 2: Force Push (ОСТОРОЖНО! ⚠️)

**Используйте ТОЛЬКО если уверены, что хотите перезаписать удаленный репозиторий!**

```powershell
git push -u origin main --force
```

⚠️ **ВНИМАНИЕ:** Это удалит все изменения на GitHub, которых нет у вас локально!

---

### Вариант 3: Если репозиторий на GitHub пустой или не нужен

Если на GitHub нет важных изменений, можно перезаписать:

```powershell
git push -u origin main --force
```

---

## 🔐 Проблема с git-credential-manager.exe

Если появляется ошибка "Неправильная строка привязки":

1. **Закройте окно ошибки**

2. **Используйте Personal Access Token вместо пароля:**
   - Зайдите на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Создайте новый token с правами `repo`
   - Используйте token как пароль при push

3. **Или настройте SSH ключ** (более безопасно)

---

## 📝 Пошаговая инструкция:

1. **Выполните в терминале:**
   ```powershell
   git pull origin main --allow-unrelated-histories
   ```

2. **Если все прошло без конфликтов:**
   ```powershell
   git push -u origin main
   ```

3. **Если есть конфликты:**
   - Разрешите конфликты в файлах
   - Затем:
   ```powershell
   git add .
   git commit -m "Merge remote changes"
   git push -u origin main
   ```
