# 🚀 Инициализация Git репозитория в папке проекта

## Проблема:
VS Code открыл репозиторий из родительской папки, где слишком много файлов.

## Решение:
Инициализировать новый Git репозиторий **именно в папке `cyberunionbot`**.

---

## Способ 1: Через Command Palette (РЕКОМЕНДУЕТСЯ) ⭐

1. **Откройте Command Palette:**
   - Нажмите `Ctrl + Shift + P`

2. **Введите и выберите:**
   ```
   Git: Initialize Repository
   ```

3. **Выберите папку:**
   - Выберите папку `cyberunionbot` (текущая рабочая папка)
   - Или просто нажмите Enter, если уже в этой папке

4. **Готово!** VS Code автоматически переключится на новый репозиторий в папке проекта.

---

## Способ 2: Через терминал VS Code

1. **Откройте терминал VS Code:**
   - Нажмите `Ctrl + ~` (тильда)
   - Или: Terminal → New Terminal

2. **Убедитесь, что вы в папке проекта:**
   ```powershell
   cd c:\Users\admin\cyberunionbot
   ```

3. **Инициализируйте репозиторий:**
   ```powershell
   git init
   ```

4. **Обновите панель Source Control:**
   - Закройте и снова откройте панель Source Control (Ctrl+Shift+G)
   - Или перезагрузите окно VS Code (Ctrl+R)

5. **Теперь в панели Source Control вы увидите только файлы вашего проекта!**

---

## После инициализации:

1. **Откройте Source Control** (Ctrl+Shift+G)
2. **Проверьте, что `.env.local` НЕТ в списке**
3. **Добавьте файлы** (нажмите "+" или "Stage All Changes")
4. **Введите сообщение коммита:**
   ```
   Fix syntax errors and update UI design
   
   - Fix JSX syntax errors in multiple pages
   - Update UI: remove neon highlights, change colors to #2222E9
   - Add logo to header
   - Improve checkbox design and alignment
   - Update button styles with hover effects
   - Fix tariff price display issues
   - Update VIP day price to 1000 rubles
   - Improve time calculation (purchase time + duration)
   - Add ErrorBoundary component
   - Update gamepad icon size
   - Improve spacing and layout
   ```
5. **Нажмите Commit** (✓)
6. **Если нужно отправить в удаленный репозиторий:**
   - Нажмите "..." → "Push"
   - Или сначала добавьте remote: `git remote add origin <URL>`

---

## ✅ Проверка:

После инициализации в папке `cyberunionbot` должна появиться скрытая папка `.git`

В Source Control должны быть видны только файлы из папки `cyberunionbot`, а не из родительских папок.
