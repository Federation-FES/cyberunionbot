# Быстрое решение проблемы "vite не найден"

## Проблема
Ошибка: `"vite" не является внутренней или внешней командой`

## Причина
1. Вы находитесь в папке `src`, а не в корне проекта
2. Зависимости проекта не установлены (нет папки `node_modules`)

## Решение (3 простых шага)

### Шаг 1: Перейдите в корень проекта

В терминале выполните:
```powershell
cd ..
```

Или полный путь:
```powershell
cd c:\Users\admin\cyberunionbot
```

### Шаг 2: Установите зависимости

```powershell
npm install
```

Это установит все необходимые пакеты, включая `vite`, в папку `node_modules`.

**Время выполнения**: 1-3 минуты (зависит от скорости интернета)

### Шаг 3: Запустите dev-сервер

```powershell
npm run dev
```

## Полная последовательность команд

Скопируйте и выполните все команды подряд:

```powershell
cd c:\Users\admin\cyberunionbot
npm install
npm run dev
```

## Что должно произойти

После `npm install` вы увидите:
```
added XXX packages in XXs
```

После `npm run dev` вы увидите:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:8080/
```

## Проверка

Убедитесь, что вы в правильной папке:
```powershell
pwd
```

Должно быть: `C:\Users\admin\cyberunionbot`

Проверьте наличие `package.json`:
```powershell
Test-Path package.json
```

Должно быть: `True`

## Если все еще не работает

1. Убедитесь, что вы в корне проекта (не в `src`)
2. Убедитесь, что `node_modules` создана после `npm install`
3. Попробуйте использовать `npx`:
   ```powershell
   npx vite
   ```

---

**После выполнения этих шагов сервер должен запуститься!** 🚀
