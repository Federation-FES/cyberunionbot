# 🔒 Безопасность и Git - Что НЕ коммитить

## ✅ Что УЖЕ защищено в .gitignore

Ваш `.gitignore` уже содержит:
- `.env.local` - файл с секретными ключами
- `.env` и `.env.production` - другие env файлы
- `*.local` - все локальные файлы
- `node_modules/` - зависимости
- `dist/` - собранные файлы

## ⚠️ КРИТИЧЕСКИ ВАЖНО - НЕ КОММИТИТЬ:

### 1. Файлы с секретами (уже в .gitignore):
- ✅ `.env.local` - содержит:
  - `VITE_ENCRYPTION_KEY` - ключ шифрования данных
  - `VITE_PAYMENT_SECRET` - секрет для платежей
  - `VITE_SUPABASE_URL` - URL вашего Supabase проекта
  - `VITE_SUPABASE_PUBLISHABLE_KEY` - публичный ключ Supabase

### 2. Проверьте перед коммитом:
```bash
# Проверьте, что .env.local НЕ в репозитории:
git ls-files | grep .env

# Если увидите .env.local в списке - СРОЧНО удалите:
git rm --cached .env.local
git commit -m "Remove .env.local from repository"
```

## 📋 Что МОЖНО коммитить:

✅ Весь код приложения (`src/`)
✅ Конфигурационные файлы без секретов:
  - `package.json`
  - `tsconfig.json`
  - `vite.config.ts`
  - `tailwind.config.ts`
✅ `.gitignore` (обязательно!)
✅ `README.md`
✅ Документация (`.md` файлы)

## 🛡️ Дополнительные меры безопасности:

### Если случайно закоммитили .env.local:

1. **Немедленно смените все ключи:**
   - Сгенерируйте новые `VITE_ENCRYPTION_KEY` и `VITE_PAYMENT_SECRET`
   - Обновите ключи в Supabase (если нужно)

2. **Удалите из истории Git:**
   ```bash
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch .env.local" \
     --prune-empty --tag-name-filter cat -- --all
   ```

3. **Принудительно обновите удаленный репозиторий:**
   ```bash
   git push origin --force --all
   ```

## ✅ Рекомендации:

1. **Перед каждым коммитом проверяйте:**
   ```bash
   git status
   ```
   Убедитесь, что `.env.local` НЕ в списке изменений!

2. **Создайте `.env.example`** (можно коммитить):
   ```env
   VITE_ENCRYPTION_KEY=your-encryption-key-here
   VITE_PAYMENT_SECRET=your-payment-secret-here
   VITE_SUPABASE_URL=your_supabase_url_here
   VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_key_here
   ```
   Это поможет другим разработчикам понять, какие переменные нужны.

3. **Используйте GitHub Secrets** для CI/CD (если используете)

## 📝 Итог:

**`.gitignore` уже настроен правильно!** Просто убедитесь, что:
- ✅ `.env.local` НЕ закоммичен
- ✅ Перед каждым `git add` проверяйте `git status`
- ✅ Никогда не добавляйте файлы с секретами вручную

Если `.gitignore` настроен правильно, Git автоматически игнорирует `.env.local` и другие секретные файлы.
