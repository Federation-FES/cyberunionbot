# ⚙️ Настройка Git пользователя

## Проблема:
Git требует настройки `user.name` и `user.email` перед первым коммитом.

## Решение:

### Способ 1: Через VS Code (РЕКОМЕНДУЕТСЯ) ⭐

1. **Откройте Command Palette:**
   - Нажмите `Ctrl + Shift + P`

2. **Настройте имя пользователя:**
   - Введите: `Git: Set User Name`
   - Нажмите Enter
   - Введите ваше имя (например: `Admin` или `Your Name`)

3. **Настройте email:**
   - Введите: `Git: Set User Email`
   - Нажмите Enter
   - Введите ваш email (например: `admin@example.com`)

4. **Готово!** Теперь можно делать коммиты.

---

### Способ 2: Через Command Palette (быстро)

1. **Откройте Command Palette** (`Ctrl + Shift + P`)

2. **Введите:**
   ```
   Git: Configure User
   ```

3. **Следуйте инструкциям** для настройки имени и email

---

### Способ 3: Через терминал (если Git установлен)

Если у вас установлен Git в системе, выполните в терминале VS Code:

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

**Или только для этого проекта:**
```powershell
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"
```

---

## ✅ Проверка:

После настройки попробуйте снова сделать коммит. Ошибка должна исчезнуть.

---

## 💡 Примечание:

- **Глобальная настройка** (`--global`) применяется ко всем репозиториям
- **Локальная настройка** (без `--global`) применяется только к текущему репозиторию
- Email может быть любым (не обязательно реальный), но лучше использовать реальный для связи с коммитами
