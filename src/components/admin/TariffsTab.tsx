import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function TariffsTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Управление тарифами</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Раздел тарифов в разработке</p>
      </CardContent>
    </Card>
  );
}